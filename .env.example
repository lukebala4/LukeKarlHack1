# Lightfern Champion Engine — env template (names only, no real values)
# Copy to `.env` and fill in. The .env file is gitignored.

# Required tech-partner stack
UNIFY_API_KEY=
ZERO_API_KEY=
ZERO_WORKSPACE_ID=
SCAILE_API_KEY=
SCAILE_API_BASE=

# Optional LLM (rule-based fallback if absent)
ANTHROPIC_API_KEY=

# Config
CHAMPION_MIN_SCORE=70
ENRICH_MIN_SCORE=50
OUTPUT_DIR=./output
